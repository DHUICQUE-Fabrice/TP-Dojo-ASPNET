@model IEnumerable<TP_Dojo.Models.ArmesSamouraisVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Samourai.Force)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Samourai.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Samourai.Arme)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Samourai.Force)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Samourai.Nom)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.Samourai.Arme.Nom)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Samourai.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Samourai.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Samourai.Id })
    </td>
</tr>
}

</table>
